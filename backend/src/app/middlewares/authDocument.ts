import type { Request, Response, NextFunction } from "express";

export const authAnalizyDoc = (req: Request, res: Response, next: NextFunction) => {

    return next()
};

export const authAnalizySelectDoc = (req: Request, res: Response, next: NextFunction) => {

    return next()
};

export const authSendDoc = (req: Request, res: Response, next: NextFunction) => {

    return next()
};

export const authDeleteDoc = (req: Request, res: Response, next: NextFunction) => {

    return next()
};